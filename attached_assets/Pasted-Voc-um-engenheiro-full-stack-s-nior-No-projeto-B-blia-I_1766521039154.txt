Você é um engenheiro full-stack sênior. No projeto “Bíblia Inteligente IA”, o sistema de planos (Gold, Premium e Strong Vitalício) JÁ EXISTE, mas os botões de compra estão bloqueados exibindo mensagens como “Em desenvolvimento” e não iniciam pagamento.

OBJETIVO:
Ativar totalmente o pagamento externo via Mercado Pago Checkout Pro, SEM quebrar nada, SEM alterar layout, SEM alterar regras de planos e SEM mexer em Google Play / App Store.

REGRAS IMPORTANTES (OBRIGATÓRIO):
1) NÃO alterar design, textos, cores ou estrutura visual das telas.
2) NÃO alterar nomes dos planos nem criar novos planos.
3) NÃO alterar regras de acesso já existentes no app.
4) NÃO remover o trial de 30 dias.
5) NÃO usar Stripe.
6) NÃO mudar nada relacionado a Google Play Billing.
7) NÃO alterar preços definidos abaixo.

PREÇOS (FIXOS – NÃO ALTERAR):
- Plano Gold: R$ 9,90 (mensal)
- Plano Premium: R$ 19,90 (mensal)
- Plano Strong Vitalício: R$ 49,90 (pagamento único)

O QUE DEVE SER FEITO:

A) FRONTEND (PASSO CRÍTICO)
- Localizar o código onde os botões “Escolher Plano” / “Assinar Agora” exibem mensagens como:
  “Em desenvolvimento”
  “Pagamento será implementado em breve”
- REMOVER esse bloqueio.
- Substituir a ação do botão para:
  → chamar uma função de assinatura real.

Criar (ou usar, se já existir) a função:
- handleSubscribe(plan)

Fluxo da função:
1) Fazer POST para `/api/mp/create-checkout`
2) Enviar no body JSON:
   { plan: "gold" | "premium" | "vitalicio" }
3) Receber `{ init_point }`
4) Redirecionar o usuário para o Mercado Pago:
   `window.location.href = init_point`

B) BACKEND
Garantir que exista e funcione corretamente:

1) Endpoint:
   POST `/api/mp/create-checkout`

2) Requisitos do endpoint:
- Usuário precisa estar autenticado
- Identificar o userId real do usuário logado
- Criar uma preferência no Mercado Pago com:
   - currency_id: "BRL"
   - quantity: 1
   - unit_price conforme o plano
   - external_reference contendo:
     { userId, plan, days } para Gold/Premium
     { userId, plan, lifetime: true } para Vitalício
- Retornar:
   { init_point }

3) Usar o Access Token armazenado em:
   process.env.MP_ACCESS_TOKEN

C) WEBHOOK (SEM ALTERAR O QUE JÁ FUNCIONA)
- Manter o endpoint:
  POST `/api/mp/webhook`
- Garantir que:
  - Extraia corretamente o paymentId
  - Consulte o pagamento real na API do Mercado Pago
  - Se status == "approved":
     • Atualize o plano do usuário no banco
     • Gold/Premium: definir validade mensal
     • Vitalício: acesso permanente ao Strong
- Adicionar logs claros no console:
  - "WEBHOOK CHEGOU"
  - "paymentId recebido"
  - "PAGAMENTO APROVADO - userId=... plan=..."

D) BANCO DE DADOS
- NÃO criar novas tabelas se já existir tabela de usuários/planos.
- Usar os campos existentes para:
  - plano atual
  - validade
  - acesso vitalício
- Se algum campo não existir, adicionar de forma mínima e segura.

E) ENTREGA
- Informar claramente:
  1) Quais arquivos foram alterados
  2) Onde o bloqueio “Em desenvolvimento” foi removido
  3) Como testar o fluxo completo:
     • clicar no plano
     • abrir checkout
     • pagar com conta de teste
     • plano liberar automaticamente

IMPORTANTE:
Este app já está publicado e funcional.
O trabalho aqui é APENAS ligar o pagamento real que já foi planejado.
Não reestruture o projeto.
Não refatore código desnecessariamente.
Não invente regras novas.
