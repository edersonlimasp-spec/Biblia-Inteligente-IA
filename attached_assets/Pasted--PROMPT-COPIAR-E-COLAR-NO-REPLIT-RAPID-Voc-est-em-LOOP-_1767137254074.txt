✅ PROMPT (COPIAR E COLAR NO REPLIT/RAPID)

Você está em LOOP de autenticação. Mesmo logado, ao abrir uma lição o app cai em “Acesso Negado / Login Necessário”. NÃO crie novas telas. NÃO invente regras novas. Corrija a causa raiz.

1) Diagnóstico obrigatório (antes de mudar)
	•	Localize onde a lição é bloqueada (route guard / middleware / componente de lição / função requireAuth / ProtectedRoute).
	•	Descubra qual condição está falhando (ex.: !user, !session, token nulo, cookie não enviado, storage vazio).
	•	Procure por:
	•	checagem de auth rodando antes de carregar o estado (ex.: loading ignorado)
	•	redirect repetido entre páginas
	•	estado de auth não persistido no refresh
	•	getSession() / onAuthStateChanged() não sendo aguardado

2) Correção correta (sem gambiarra)

Implemente um fluxo padrão e único:

Regra:
	•	Enquanto o auth estiver carregando: mostrar Loading (não negar acesso).
	•	Se terminou de carregar e não há usuário: mostrar a tela “Login Necessário”.
	•	Se há usuário válido: abrir a lição normalmente.

Obrigatório:
	•	Criar/ajustar AuthProvider (ou equivalente) com:
	•	user
	•	loadingAuth (começa true, vira false após checar sessão)
	•	Atualizar o ProtectedRoute para respeitar loadingAuth.

Exemplo do comportamento esperado (não copie literal se sua stack for diferente):
	•	if (loadingAuth) return <Loading />
	•	if (!user) return <AccessDenied />
	•	return children

3) Persistência da sessão (o principal)

Corrigir para que a sessão continue válida:
	•	Se usa Supabase/Firebase/Auth0/NextAuth/etc:
	•	garantir que a sessão é lida no start do app
	•	garantir que cookies/headers são enviados no ambiente mobile/webview (TWA)
	•	Se usa token em localStorage:
	•	garantir que grava após login
	•	garantir que lê antes de validar rota
	•	não limpar storage sem motivo

4) Remover o LOOP
	•	Se existe redirect automático para /login e outro redirect de volta, pare isso.
	•	A página “Login Necessário” deve ter botão “Entrar” (ou “Ir para Login”), e não pode redirecionar sozinha em loop.
	•	Se o usuário já está logado, essa página não pode aparecer.

5) Teste obrigatório no Preview (antes de concluir)

No preview, faça exatamente estes testes e só finalize se passar:
	1.	Login
	2.	Abrir Cursos Premium
	3.	Abrir uma lição
	4.	Atualizar a página (refresh) e abrir a lição de novo
	5.	Fazer logout e tentar abrir lição → deve cair em “Login Necessário”
	6.	Logar novamente → deve abrir a lição

6) Entrega

Ao final, me informe:
	•	arquivo(s) alterados
	•	qual era a condição que falhava
	•	como você garantiu que não existe mais loop
	•	prints/descrição dos testes concluídos no preview

Reforço: NÃO crie telas novas para esconder o problema. Corrija o guard de rota e o carregamento/persistência da sessão.
