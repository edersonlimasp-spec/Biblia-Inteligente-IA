VOCÊ É O REPLIT AGENT. O BUG É SIMPLES:
NO APP PUBLICADO, quando eu TROCO a VERSÃO da Bíblia no seletor, o texto NÃO MUDA (fica a mesma versão).
No PREVIEW funciona. No DEPLOY NÃO.

NÃO CRIE TELAS NOVAS. NÃO REFAÇA O APP. CORRIJA APENAS O FLUXO DO SELETOR + CARREGAMENTO DO TEXTO.

OBJETIVO
1) Ao trocar a versão, o texto deve mudar imediatamente.
2) Deve persistir a versão escolhida (refresh mantém).
3) Deve funcionar NO DOMÍNIO PUBLICADO (.replit.app), não só no Preview.
4) Se der erro ao carregar, mostrar aviso e manter a última versão válida (sem tela branca).

────────────────────────────────────────
PASSO 1 — COLOCAR LOGS E ACHAR ONDE TRAVA (OBRIGATÓRIO)
Adicione logs temporários (não remova até funcionar) para descobrir qual parte falha:

A) No onChange do seletor de versão:
console.log("[VERSION_CHANGE]", {from: currentVersion, to: newVersion, lang, book, chapter, origin: window.location.origin});

B) No lugar onde você guarda o estado (context/store/state):
console.log("[VERSION_STATE_APPLIED]", {version: state.version, lang: state.lang});

C) Na função que carrega o texto (fetch/getBibleText/loadChapter):
console.log("[BIBLE_LOAD_START]", {version, lang, book, chapter});
console.log("[BIBLE_LOAD_URL]", url);
console.log("[BIBLE_LOAD_RESULT]", {status, ok, length});
console.error("[BIBLE_LOAD_ERROR]", error);

Agora rode NO DEPLOY e responda no console/logs qual é o cenário:
- (1) VERSION_CHANGE aparece, mas VERSION_STATE_APPLIED NÃO muda (estado não atualiza)
- (2) VERSION_STATE_APPLIED muda, mas BIBLE_LOAD_* não roda (não dispara reload)
- (3) BIBLE_LOAD roda, mas retorna sempre o mesmo conteúdo (cache/URL fixa/params ignorados)
- (4) BIBLE_LOAD falha (404/CORS/500) e o app mantém texto antigo

────────────────────────────────────────
PASSO 2 — CORRIGIR DE FORMA DEFINITIVA (SEM CHUTE)
Implemente estas regras obrigatórias:

REGRA 1: “Versão” TEM que fazer parte da chave de carregamento
- A função de carregar texto deve usar version/lang/book/chapter como parâmetros reais.
- A URL ou query precisa incluir a versão (não pode ser fixa).
Exemplo conceitual:
getBibleText({version, lang, book, chapter})

REGRA 2: “Trocar versão” TEM que disparar reload
- Quando newVersion for selecionada:
  1) atualizar o estado global (context/store)
  2) salvar em localStorage
  3) chamar explicitamente o loader OU garantir que useEffect observe a versão e recarregue

Você deve garantir que existe um gatilho assim:
- useEffect(() => { loadText(version, lang, book, chapter) }, [version, lang, book, chapter])
OU disparo direto após setVersion (com cuidado para pegar o valor novo)

REGRA 3: Corrigir cache no Deploy (muito comum)
Se o fetch no deploy está retornando sempre igual por cache:
- Adicione cache-busting:
  - na URL: ?v=VERSION&ts=Date.now()
  - e headers: cache: "no-store" (ou equivalente)
Exemplo:
fetch(url, { cache: "no-store" })

REGRA 4: Persistência
- Ao trocar versão:
localStorage.setItem("bible_version", newVersion)
- No boot:
const saved = localStorage.getItem("bible_version") || default
aplicar antes de renderizar o texto

REGRA 5: Se falhar carregar
- Mostrar mensagem: “Não foi possível carregar esta versão agora.”
- Não limpar o texto atual (manter última versão válida)

────────────────────────────────────────
PASSO 3 — TESTE NO DOMÍNIO PUBLICADO (OBRIGATÓRIO)
No deploy (.replit.app) faça:
1) Trocar versão 3 vezes e confirmar que o texto MUDA (visível)
2) Refresh e manter a versão escolhida
3) Trocar livro/capítulo e confirmar que mantém a versão

Só finalize se passar.

ENTREGA
Diga:
- qual era a falha (estado não mudava? não recarregava? cache? URL ignorava versão?)
- quais arquivos alterou
- prova: logs do deploy mostrando:
  [VERSION_CHANGE]
  [VERSION_STATE_APPLIED] com nova versão
  [BIBLE_LOAD_URL] contendo a versão
  [BIBLE_LOAD_RESULT] status 200 e length diferente quando muda versão

IMPORTANTE
- NÃO criar telas novas
- NÃO mexer em outras regras do app
- FOCO: troca de versão precisa mudar o texto no DEPLOY