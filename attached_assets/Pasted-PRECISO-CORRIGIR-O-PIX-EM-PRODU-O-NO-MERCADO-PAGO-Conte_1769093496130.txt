PRECISO CORRIGIR O PIX EM PRODUÇÃO NO MERCADO PAGO.

Contexto:
- Hoje o app cria Checkout Pro (preference + init_point redirect). Isso funciona para cartão, mas NÃO gera QR Pix dentro do app.
- Eu preciso que o Pix seja gerado via API do Mercado Pago em tempo real e exibido em um modal no app (QR Code + copia e cola).
- O MP_ACCESS_TOKEN já existe nos Secrets do Replit e deve ser usado via process.env.MP_ACCESS_TOKEN.

Objetivo:
1) Manter o fluxo atual de CARTÃO via Checkout Pro (preference -> init_point redirect).
2) Criar um novo fluxo separado para PIX que NÃO usa preference/init_point.
3) No PIX, criar pagamento via endpoint /v1/payments com payment_method_id="pix" e retornar:
   - qr_code_base64 (para renderizar imagem do QR)
   - qr_code (copia e cola)
   - payment_id (para acompanhar status)
4) No front-end, no botão "Pagar com Pix", abrir modal e:
   - chamar o endpoint do backend (ex: POST /api/mp/pix)
   - exibir o QR (base64) e um botão "Copiar código Pix"
   - exibir um status "Aguardando pagamento..."
5) Criar uma rota para consultar status do pagamento (ex: GET /api/mp/payment/:id) que chama Mercado Pago e retorna status.
6) Quando o status virar "approved", ativar o plano do usuário no sistema (mesma lógica usada hoje no webhook/retorno do checkout), sem depender do redirect.

Requisitos técnicos:
- Usar o mesmo MP_ACCESS_TOKEN já existente em Secrets.
- Não quebrar o fluxo de cartão já existente.
- Implementar logs claros no console:
  - [MP] Pix criado: paymentId=...
  - [MP] Pix status: paymentId=... status=...
- Validar erros e retornar mensagens amigáveis.

Implementação (Node/Express):
- Criar arquivo/serviço mpPix.ts (ou mpPix.js) para concentrar:
  - createPixPayment(amount, description, payerEmail, metadata)
  - getPaymentStatus(paymentId)
- Endpoint:
  POST /api/mp/pix
  body: { planId, userId, email }
  retorna: { qrCodeBase64, qrCode, paymentId }
- Endpoint:
  GET /api/mp/payment/:paymentId
  retorna: { status, status_detail }

Front-end:
- Atualizar página /planos (ou componente dos planos):
  - Botão "Pagar com Pix" -> abre modal
  - Modal exibe QR + copia e cola + botão copiar
  - Fazer polling a cada 3-5 segundos chamando /api/mp/payment/:id
  - Quando aprovado, mostrar sucesso e liberar acesso/atualizar UI

IMPORTANTE:
- Eu NÃO quero usar init_point/checkout redirect para Pix.
- Quero QR Code no app.
- Se o projeto usa SDK do Mercado Pago, use a versão moderna (MercadoPagoConfig + Payment). Se não, use fetch/axios direto na API.
- Garanta que funciona em PRODUÇÃO.
- Após alterações, garantir que o build/publish não quebre e orientar para "Republish" no Replit.

Faça as mudanças no código inteiro necessárias e me diga quais arquivos foram alterados/criados.
