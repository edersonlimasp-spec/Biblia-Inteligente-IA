URGENTE: meu app “Bíblia Inteligente” está FORA em produção e clientes estão reclamando.
Preciso correção IMEDIATA de dois pontos:

1) PRODUÇÃO NÃO ABRE / BUILD FALHA
2) BÍBLIA STRONG PAROU DE FUNCIONAR APÓS PUBLISH

NÃO considere pronto até validar os dois em PRODUÇÃO real.

====================================================
1) CORRIGIR BUILD / DEPLOYMENT EM PRODUÇÃO
====================================================
Sintoma:
- Publicação falha com erro tipo: “vite não foi encontrado” / deps não instaladas antes do build.
- URL pública não carrega.

Ações obrigatórias:
1. Verificar package.json (raiz e client/ se houver monorepo):
   - confirmar que "vite" está em DEVDEPENDENCIES no pacote certo (onde roda o build do front).
   - confirmar scripts:
     "build": "vite build" (ou equivalente)
     "start": "node server.js" (ou "npm run serve")

2. Ajustar configuração de deploy do Replit:
   - Build command DEVE ser:
     npm ci && npm run build
     (se existir client/, usar: npm ci && npm run build --workspace client OU cd client && npm ci && npm run build)
   - Run command DEVE ser:
     npm run start
     (garantir que start sobe o backend e serve o dist/)

3. Garantir instalação de deps em produção:
   - se houver .replit / replit.nix / deployment config:
     incluir passo de install antes do build.
   - commits nessas configs se necessário.

4. Servir front estático corretamente:
   - Vite gera /dist no client:
     backend deve servir dist:
       app.use(express.static(path.join(__dirname, "dist")));
       app.get("*", (req,res)=>res.sendFile(path.join(__dirname,"dist","index.html")));

5. Garantir porta correta:
   - usar process.env.PORT
   - backend deve escutar:
     app.listen(process.env.PORT || 5000)

6. Depois do ajuste:
   - Republish e confirmar que https://bibliainteligente.replit.app abre sem erro.

====================================================
2) STRONG PAROU APÓS PUBLISH (CORRIGIR)
====================================================
Sintoma:
- no preview/dev funciona ou funcionava,
- após publish: clicar nas palavras não abre Strong / não busca definição.

Ações obrigatórias:
1. Validar DADOS em produção:
   - checar se tabela/arquivo Strong está acessível em PROD:
     • se usa DB: confirmar DATABASE_URL PROD e migrations rodadas
     • se usa JSON/local: confirmar que foi incluído no build e está em path correto

2. Validar API Strong em produção:
   - testar endpoint real:
     GET /strong/:id
   - garantir que retorna definição ok no ambiente publicado.

3. Validar frontend tokenização:
   - confirmar que o parser/tokenizer continua devolvendo strongId por palavra no build de produção.
   - procurar por transform que remove strongId no map() ou build optimizer.

4. Corrigir handler de clique:
   - cada palavra com strongId deve ser clicável:
     onClick/onPress -> abrir modal/drawer
     -> fetch /strong/:strongId
   - palavras sem strongId NÃO podem abrir modal.

5. Acrescentar “destaque sutil” nas palavras com Strong:
   - sem poluir leitura:
     • palavra levemente mais escura (+10% contraste)
     • underline dotted fino
   - aplicar APENAS se strongId existir.

Exemplo Tailwind/CSS:
- className condicional:
  hasStrong
   ? "font-medium underline decoration-dotted decoration-1 underline-offset-2 text-slate-900"
   : "text-slate-800"

====================================================
3) TESTES OBRIGATÓRIOS (ANTES DE DECLARAR PRONTO)
====================================================
Testar EM PRODUÇÃO (URL publicada):
1. Abrir app publicado.
2. Entrar em 3 textos:
   - AT (hebraico)
   - NT (grego)
   - salmos/evangelhos (muitas ocorrências)
3. Clicar em várias palavras com Strong:
   ✅ abre modal
   ✅ mostra Strong Hxxxx/Gxxxx, original, transliteração, definição
4. Palavras sem Strong:
   ✅ não abre nada
5. Destaque sutil aparece somente onde tem Strong.

====================================================
4) ENTREGA
====================================================
Depois de corrigir, você deve me entregar:
- quais arquivos alterou
- como ficou o build command/run command
- prints ou descrição do teste em produção
- confirmação de que Strong funciona após publish.

NÃO diga “está pronto” sem validar tudo acima.
