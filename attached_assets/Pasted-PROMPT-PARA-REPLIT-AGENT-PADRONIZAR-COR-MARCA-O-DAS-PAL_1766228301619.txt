PROMPT PARA REPLIT (AGENT) — PADRONIZAR COR/MARCAÇÃO DAS PALAVRAS COM STRONG EM TODA A BÍBLIA (SEM MUDAR LAYOUT)

OBJETIVO
Padronizar a marcação visual (cor/estilo) de TODAS as palavras que possuem dicionário Strong, garantindo consistência em:
- Antigo Testamento (hebraico)
- Novo Testamento (grego)
- TODOS os livros, capítulos e versículos
E garantir que: toda palavra marcada é realmente clicável e abre o Strong correto.

REGRAS IMPORTANTES
1) NÃO alterar o layout da página (estrutura, tamanhos, espaçamentos, header/footer, componentes).
2) NÃO mudar as cores gerais do tema do app. Apenas padronizar a cor/estilo das palavras com Strong.
3) A marcação deve ser sutil e igual em toda a Bíblia (sem variações por livro/capítulo/versão).
4) Palavra marcada = palavra clicável = sempre abre o Strong (sem falhas).
5) Palavra não marcada = não deve abrir Strong (evitar falso positivo).

PADRÃO VISUAL (DEFINIÇÃO)
Escolher UM único padrão e aplicar em 100% dos textos:
- Cor: manter a cor atual definida para Strong (a mesma azul/acizentada do app) OU um tom sutil equivalente já usado.
- Estilo adicional (se já aprovado): traço bem sutil nas 3 primeiras letras (ou sublinhado leve), mas SEM mexer em fonte/tamanho.
OBS: Não inventar cores novas. Usar exatamente a cor padrão do Strong já existente no projeto.

IMPLEMENTAÇÃO TÉCNICA (OBRIGATÓRIA)
A) Unificar a lógica de “tokenização” e marcação
1) Criar/centralizar uma função única para renderizar versículos, ex:
- tokenizeVerseText(text, strongMap) -> tokens com:
  - text
  - hasStrong (boolean)
  - strongNumber (string)
2) Garantir que essa função é usada em TODOS os lugares onde o versículo aparece:
- leitura normal
- resultados de busca
- compartilhamento/preview de versículo
- telas de estudo, etc.

B) Unificar o componente de palavra clicável
1) Criar um componente reutilizável, ex: <StrongWord />
- Recebe: token.text, token.hasStrong, token.strongNumber
- Se hasStrong=true:
  - aplica classe CSS padrão (ex: .strong-word e opcional .strong-mark)
  - onClick abre Strong usando strongNumber
- Se hasStrong=false:
  - render normal, sem onClick

C) CSS único e global
1) Colocar o estilo padronizado em um local global (ex: globals.css / tailwind config)
2) Garantir que não existam estilos conflitantes por página:
- remover variações locais (classes duplicadas diferentes em páginas diferentes)
- manter 1 fonte da verdade

D) Garantia de clique fiel
1) Ao clicar, abrir Strong pelo strongNumber (não por texto da palavra)
2) Validar que o strongNumber retornado existe no DB (se não existir, não marcar como Strong)
3) Tratamento de erro:
- se falhar, exibir mensagem “Conteúdo Strong indisponível, tente novamente” (não silencioso)

VALIDAÇÃO OBRIGATÓRIA (ANTES DE ENTREGAR COMO PRONTO)
1) Testar em produção/publicado e preview:
- Gênesis (AT): palavras marcadas aparecem e abrem Strong
- Salmos (AT): idem
- Mateus (NT): idem
- João (NT): idem
- Romanos (NT): idem
2) Confirmar que a cor/estilo das palavras com Strong é IGUAL em todos os testes.
3) Confirmar que NÃO mudou nada do layout fora da marcação das palavras.

ENTREGA
- Informar quais arquivos foram alterados
- Confirmar que a marcação foi padronizada globalmente
- Confirmar que o clique funciona em todos os livros/capítulos/versículos no publicado