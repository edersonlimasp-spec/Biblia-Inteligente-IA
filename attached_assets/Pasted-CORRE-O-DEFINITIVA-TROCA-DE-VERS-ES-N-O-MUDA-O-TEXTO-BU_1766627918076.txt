CORREÇÃO DEFINITIVA — TROCA DE VERSÕES NÃO MUDA O TEXTO (BUG CRÍTICO)
NÃO FAÇA WORKAROUND VISUAL. NÃO REMOVA VERSÕES. NÃO CONSIDERE CONCLUÍDO SEM PROVA.

SINTOMA:
O usuário seleciona outra versão (ACF/ARC/KJV etc), mas o texto exibido NÃO MUDA.

OBJETIVO:
1) Ao trocar a versão, o app deve buscar e renderizar o capítulo da versão selecionada.
2) O request deve incluir translationId/versionId e isso deve chegar no backend.
3) O backend deve consultar a fonte correta (DB/JSON/API) para essa tradução.
4) Cache (client/server/service worker/CDN) NÃO pode devolver sempre a mesma resposta.
5) Funcionar em PREVIEW e PUBLICADO.

=====================================================
FASE 1 — DIAGNÓSTICO OBRIGATÓRIO (ANTES DE MUDAR CÓDIGO)
=====================================================
A) Identifique exatamente:
- Qual componente controla o dropdown de versões (arquivo e função).
- Qual state armazena a versão selecionada (store/hook/localStorage).
- Qual função faz o fetch do capítulo (arquivo e rota).
- Qual endpoint entrega o capítulo (ex: /api/bible/chapter).
- Se existe Service Worker/Workbox/PWA cacheando chamadas.

B) ADICIONE LOGS OBRIGATÓRIOS:
1) FRONTEND: no evento onChange do dropdown:
   console.log("[BIBLE] version changed ->", { translationId, book, chapter, ts: Date.now() })
2) FRONTEND: antes do fetch do capítulo:
   console.log("[BIBLE] fetching ->", urlCompletaComQueryParams)
3) BACKEND: no endpoint que retorna capítulo:
   console.log("[API] chapter request ->", { translationId, book, chapter, cacheHit, ts: Date.now() })
   Se translationId estiver ausente: retornar 400 com mensagem clara.

C) CRIE ENDPOINTS DE DEBUG (OBRIGATÓRIO):
1) GET /api/debug/bible-chapter?translationId=pt-acf&book=gen&chapter=1
Retornar:
{
  translationId,
  book,
  chapter,
  verse1: "texto do versículo 1",
  source: "db|json|api",
  cacheKey: "...",
  buildVersion: "..."
}
2) GET /api/debug/build-version
Retornar buildTime + hash + environment (dev/prod).

D) TESTE RÁPIDO (VOCÊ DEVE RODAR):
- Trocar ACF -> ARC -> KJV e chamar /api/debug/bible-chapter para cada uma.
- Se verse1 voltar IGUAL para todas, o problema é: translationId ignorado OU fonte é única.

=====================================================
FASE 2 — CORREÇÃO DEFINITIVA (SEM REMOVER VERSÕES)
=====================================================
1) GARANTA QUE translationId FAZ PARTE DA CHAVE DE DADOS:
- Se usa React Query/SWR/cache manual:
  queryKey deve incluir: ["chapter", translationId, book, chapter]
- Se usa memoization:
  dependências devem incluir translationId.

2) GARANTA QUE O REQUEST MUDA:
- O fetch deve chamar endpoint com translationId:
  /api/bible/chapter?translationId=XXX&book=gen&chapter=1
- Proibido fallback silencioso para versão padrão.

3) GARANTA QUE O BACKEND RESPEITA translationId:
- No endpoint, usar translationId para buscar tabela correta:
  - bible_verses com coluna translationId
  - OU tabela translations + join
  - OU map para arquivos JSON separados por tradução
- Se não existir tradução solicitada: retornar 404 + mensagem.

4) CORRIGIR CACHES (PRINCIPAL CAUSA EM PRODUÇÃO):
A) SERVER:
- Adicionar header para respostas de capítulo:
  Cache-Control: no-store (pelo menos até estabilizar)
  ETag opcional.
B) SERVICE WORKER/PWA:
- Se houver, REMOVER cache de chamadas /api/bible* ou
  usar NetworkFirst SEM fallback para cache antigo.
- Forçar update de SW:
  - increment version
  - skipWaiting + clientsClaim
  - e mostrar “Atualização disponível” se necessário.
C) FRONTEND BUILD:
- Garantir que a publicação está servindo o build novo (usar /api/debug/build-version).

5) PERSISTÊNCIA:
- Salvar translationId em localStorage e restaurar no load.
- Se logado, salvar em user_preferences no backend.
- Em qualquer tela, a versão deve ser consistente.

=====================================================
FASE 3 — PROVA EM PRODUÇÃO (OBRIGATÓRIA)
=====================================================
Você deve me entregar:
1) Print/registro de logs (ou copiar logs) mostrando:
   - onChange selecionou translationId diferente
   - fetch chamou URL diferente
   - backend recebeu translationId diferente
2) Em PRODUÇÃO, para GEN 1:
   - ACF verse1 != ARC verse1 != KJV verse1 (no endpoint debug e na UI)
3) Listar arquivos alterados (paths).
4) Confirmar que Strong continua abrindo após trocar versão.

NÃO FINALIZE. NÃO COBRE. NÃO “CONCLUÍDO”:
Se não houver prova via /api/debug/bible-chapter e logs reais.