PROMPT PARA REPLIT (AGENT) — CORRIGIR CONFLITO “PROFESSOR x ANOTAÇÕES” + BOTÃO EXCLUIR ANOTAÇÃO

CONTEXTO
Existe um conflito de estado/navegação entre o módulo “Professor” (IA) e o módulo “Anotações”.

PROBLEMA 1 (CRÍTICO) — CONFLITO AO VOLTAR PARA O VERSÍCULO
Passos que causam o bug:
1) Estou lendo a Bíblia e uso o “Professor” (IA).
2) Vou para “Anotações”.
3) Dentro de Anotações, clico para “voltar ao versículo” (abrir o versículo anotado).
RESULTADO ATUAL (ERRADO):
- Ao abrir o versículo, o app “puxa” a ÚLTIMA PERGUNTA/RESPOSTA do Professor,
- e o painel do Professor fica “fora do lugar”/bugado na tela de leitura.
O “retorno ao versículo” deve abrir somente o texto do versículo e a UI normal da leitura.

RESULTADO ESPERADO (CORRETO):
- Ao voltar ao versículo a partir de Anotações:
  ✅ abrir o livro/capítulo/versículo correto
  ✅ manter a tela de leitura normal e estável
  ✅ o Professor deve iniciar “limpo” (sem puxar a última conversa automaticamente)
  ✅ não pode haver sobreposição de componentes nem estado quebrado
  ✅ NÃO perder o layout atual

CAUSA PROVÁVEL
Estado global compartilhado/centralizado (ex: store/context) do Professor sendo reaproveitado ao navegar.
Ou navegação “back” recuperando a tela anterior com estado antigo (SPA history) sem reset.
Ou componente do Professor sendo montado com state persistente indevido.

CORREÇÃO OBRIGATÓRIA (SEM QUEBRAR O QUE JÁ EXISTE)
A) Separar o estado do Professor do fluxo de “abrir versículo”
- Ao navegar de Anotações para Leitura, NÃO carregar histórico automaticamente do Professor.
- Resetar state do Professor ao entrar na tela de leitura vindo de Anotações:
  - limpar input
  - limpar thread atual (se for regra do app: “ao fechar e abrir reinicia conversa”)
  - manter apenas “histórico lateral” se existir, mas NÃO reabrir a última conversa automaticamente.

B) Usar parâmetros de rota para abrir versículo
- Ao clicar em “ver versículo” na anotação, navegar para:
  /biblia?book=XX&chapter=Y&verse=Z&source=notes
- Detectar source=notes e executar:
  - carregar versículo
  - reset do Professor (apenas nesse caso)

C) Garantir que a UI do Professor não “vaze”
- Verificar z-index, position, e render condicional
- Garantir que o painel do Professor é renderizado somente dentro do container correto da Bíblia
- Garantir unmount/mount correto ao trocar telas

D) Teste obrigatório
- Fazer o fluxo acima e confirmar que não volta a última pergunta e não quebra a tela.

========================================================
PROBLEMA 2 — BOTÃO PARA EXCLUIR ANOTAÇÕES
========================================================
Dentro do módulo “Anotações” adicionar ação de excluir:

REQUISITOS
1) Em cada anotação na lista, adicionar botão “Excluir” (ícone lixeira) discreto.
2) Ao clicar, abrir confirmação:
   “Deseja excluir esta anotação? Essa ação não pode ser desfeita.”
   [Cancelar] [Excluir]
3) Após excluir:
   - remover da lista imediatamente (UI atualiza sem recarregar a página)
   - remover do banco/armazenamento persistente
4) Se falhar, mostrar mensagem de erro clara.

IMPLEMENTAÇÃO
- Criar endpoint DELETE /api/notes/:id (protegido por login)
- Atualizar frontend para chamar esse endpoint e atualizar state local
- Garantir que um usuário só exclui a própria anotação

========================================================
ENTREGAS / VALIDAÇÃO
========================================================
1) Corrigir o conflito Professor x Anotações:
- Ao voltar ao versículo, Professor deve iniciar limpo e a tela deve ficar estável.
2) Implementar botão de excluir anotação + confirmação.
3) Testar em Preview e Publicado.

Ao final, me informe:
- quais arquivos alterou
- como o reset do Professor foi implementado (quando source=notes)
- confirmação que o bug não acontece mais.