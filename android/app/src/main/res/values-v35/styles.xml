<?xml version="1.0" encoding="utf-8"?>
<!--
    Estilos específicos para Android 15 (API 35).
    No Android 15, apps com targetSdkVersion=35 são forçados ao modo Edge-to-Edge.
    Este arquivo garante conformidade e remove o aviso do Google Play Console.
-->
<resources>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Edge-to-Edge completo no Android 15 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Confirma adesão ao Edge-to-Edge (não opt-out) -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item>
    </style>

</resources>
