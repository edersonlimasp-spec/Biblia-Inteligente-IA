PROMPT URGENTE — BUG PERSISTENTE: CLICAR EM ANOTAÇÃO ABRE ÚLTIMA CONVERSA DA IA

O bug AINDA EXISTE em produção e preview:
- Ao entrar em ANOTAÇÕES e clicar em uma anotação, o app NÃO abre a anotação.
- Ele redireciona/abre a ÚLTIMA conversa do “Professor” (IA).

Isso significa que:
✅ O click handler da anotação está chamando a rota/handler do chat
OU
✅ Existe um state global compartilhado (Context/Zustand/Redux) que sobrescreve a view
OU
✅ O router está reusando o mesmo componente/Outlet para ambos e renderizando o chat por engano.

NÃO QUERO MAIS "tentativas". Quero depuração com prova.

=========================
1) REPRODUÇÃO + INVESTIGAÇÃO
=========================
- Adicione logs (console) TEMPORÁRIOS e claros:
  A) no onClick do card da anotação (log: NOTE_CLICKED, noteId)
  B) no router/navigate (log: NAVIGATE_TO, targetPath)
  C) no componente do Professor/Chat (log: CHAT_MOUNTED, reason/route)
  D) no componente da anotação (log: NOTE_DETAIL_MOUNTED, noteId)

- Execute o fluxo:
  1) Faça 1 pergunta na IA
  2) Vá para Anotações
  3) Clique em uma anotação
- Mostre no log qual caminho foi executado e onde está o redirecionamento errado.

=========================
2) CORREÇÃO ESTRUTURAL OBRIGATÓRIA
=========================
A) ROTAS SEPARADAS (SEM COMPARTILHAR VIEW)
- Criar rotas dedicadas e exclusivas:
  /notes                 -> lista
  /notes/:id             -> detalhe (abre a anotação)
  /bible?book=...        -> leitura
  /professor             -> chat IA (se existir rota separada)

- Garantir que /notes/:id NÃO reutiliza componente/Outlet do chat.
- Garantir que o chat não é renderizado em /notes/:id.

B) STATE ISOLADO POR DOMÍNIO
- O estado do chat (thread, messages, lastQuestion) não pode estar em store global compartilhado com Anotações.
- Se existir store global, separar em:
  useChatStore / ChatContext
  useNotesStore / NotesContext
- Remover qualquer efeito que faça "restore last chat" ao montar telas que não são do chat.

C) CLICK HANDLER CORRETO
- No card de anotação, o onClick deve ser EXATAMENTE:
  navigate(`/notes/${note.id}`)
- Proibir qualquer fallback do tipo:
  navigate("/professor") ou navigate("/bible") ou restoreLastRoute()

D) BLOQUEAR RESTORE AUTOMÁTICO DO CHAT
- Se existir lógica de "restaurar última conversa" ao montar a tela Bíblia,
  ela deve rodar SOMENTE quando o usuário entrar deliberadamente no módulo Professor.
- Ao entrar em Anotações ou abrir /notes/:id:
  - chat NÃO deve restaurar
  - chat NÃO deve montar
  - chat NÃO deve sobrescrever UI

=========================
3) TESTE AUTOMATIZADO (OBRIGATÓRIO)
=========================
Criar um teste simples (Playwright/Cypress ou teste de navegação):
- simular: abrir chat -> enviar msg -> abrir anotações -> clicar anotação
- assert:
  - URL contém /notes/
  - elemento do conteúdo da anotação aparece (ex: data-testid="note-content")
  - elemento do chat NÃO aparece (ex: data-testid="chat-panel")

=========================
4) PROVA EM PRODUÇÃO
=========================
Após publicar:
- repetir fluxo
- confirmar que:
  - clicar anotação abre o texto correto
  - NÃO aparece a última pergunta/resposta da IA
  - o chat não monta em /notes/:id

REGRA: NÃO CONCLUIR E NÃO COBRAR ATÉ:
✅ logs confirmarem correção
✅ teste automatizado passar
✅ funcionar na URL publicada