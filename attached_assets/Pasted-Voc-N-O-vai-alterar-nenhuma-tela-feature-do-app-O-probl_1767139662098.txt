Você NÃO vai alterar nenhuma tela/feature do app. O problema é somente no DEPLOY.

Erro atual no Deploy: “npm install failed during build with network error (ECONNRESET)”, “Network connectivity issue preventing package installation”, “sh build-production.sh failed because dependencies could not be downloaded”.

Objetivo: Fazer o Deploy buildar SEM depender de rede instável durante o build.

FAÇA EXATAMENTE ESTES PASSOS:

1) Verifique o arquivo build-production.sh (ou o comando de build configurado no Deploy).
   - Identifique onde está rodando `npm install` ou `npm ci`.
   - Garanta que o build use `npm ci` (não npm install) quando existir package-lock.json.

2) Ajuste o build para usar cache e tolerância a rede:
   - Configure npm para aumentar retries e timeouts ANTES de instalar:
     - `npm config set fetch-retries 5`
     - `npm config set fetch-retry-mintimeout 20000`
     - `npm config set fetch-retry-maxtimeout 120000`
     - `npm config set network-timeout 120000`
   - E rode `npm ci --prefer-offline --no-audit --no-fund`

3) Fixe o registry explicitamente (evita resolução errada):
   - `npm config set registry https://registry.npmjs.org/`

4) Se o projeto usar Node, fixe versão compatível no deploy:
   - Adicione/ajuste `.nvmrc` (ex: 20) OU engines no package.json:
     - "engines": { "node": ">=20 <21", "npm": ">=10" }
   - Não mudar dependências sem necessidade.

5) Limpeza controlada:
   - Remova node_modules e limpe cache somente se necessário:
     - `rm -rf node_modules`
     - `npm cache clean --force`
   - Depois gere/valide que package-lock.json existe e está consistente.

6) Atualize o comando de build no Deploy para algo direto (sem scripts frágeis):
   - Build: `npm ci --prefer-offline --no-audit --no-fund && npm run build`
   - Start/Run: `npm run start` (ou o comando correto do app)
   - Se for Next/Vite/React, use os scripts corretos do package.json.

7) Antes de concluir:
   - Rode o build no ambiente do Replit e mostre o resultado no console.
   - Em seguida tente Deploy novamente e confirme que passou do passo “npm install”.

Entregáveis:
- Quais arquivos você alterou (ex: build-production.sh, .replit, package.json, .nvmrc)
- O comando final de Build e Run no Deploy
- O log final mostrando que não falha mais em npm install
